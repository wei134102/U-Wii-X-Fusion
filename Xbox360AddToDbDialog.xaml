<Window x:Class="U_Wii_X_Fusion.Xbox360AddToDbDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加到 Xbox 360 游戏数据库" Height="260" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="游戏ID（Title ID）：" Margin="0,0,0,4"/>
        <TextBox x:Name="txtTitleId" Grid.Row="1" Margin="0,0,0,12" Padding="4,2"/>
        <TextBlock Grid.Row="2" Text="游戏名称（英文）：" Margin="0,0,0,4"/>
        <TextBox x:Name="txtName" Grid.Row="3" Margin="0,0,0,12" Padding="4,2"/>
        <TextBlock Grid.Row="4" Text="中文名称：" Margin="0,0,0,4"/>
        <TextBox x:Name="txtChineseName" Grid.Row="5" Margin="0,0,0,16" Padding="4,2"/>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="btnOk" Content="确定" Width="80" Padding="6,3" Margin="0,0,10,0" IsDefault="True" Click="BtnOk_Click"/>
            <Button Content="取消" Width="80" Padding="6,3" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
