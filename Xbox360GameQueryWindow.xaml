<Window x:Class="U_Wii_X_Fusion.Xbox360GameQueryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Xbox 360 游戏数据库"
        Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Background="#F5F5F5" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Xbox 360 游戏数据库" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,15" Background="White">
            <StackPanel>
                <TextBlock Text="搜索" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBox x:Name="txtSearch" Width="260" Margin="0,0,10,0" ToolTip="输入游戏ID或名称"/>
                    <TextBlock Text="分类：" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <ComboBox x:Name="cmbCategory" Width="120" MinWidth="100" SelectionChanged="CmbCategory_SelectionChanged"/>
                    <Button x:Name="btnSearch" Content="搜索" Width="80" Margin="10,0,0,0" Click="BtnSearch_Click"/>
                    <Button x:Name="btnClearSearch" Content="清除" Width="80" Margin="10,0,0,0" Click="BtnClearSearch_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="游戏列表" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="txtGameCount" Text="" FontSize="12" Foreground="#666" Margin="12,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <DataGrid x:Name="dgGames" Grid.Row="1" AutoGenerateColumns="False" MinHeight="200" Background="White"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          SelectionChanged="DgGames_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="游戏ID" Width="100" Binding="{Binding GameId}"/>
                        <DataGridTextColumn Header="游戏名称" Width="*" Binding="{Binding Title}"/>
                        <DataGridTextColumn Header="中文名称" Width="120" Binding="{Binding ChineseTitle}"/>
                        <DataGridTextColumn Header="分类" Width="80" Binding="{Binding Category}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Border Grid.Column="1" Margin="20,0,0,0" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="10" Background="White">
                <StackPanel>
                    <TextBlock Text="游戏详情" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="spGameDetails" MinHeight="120"/>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
